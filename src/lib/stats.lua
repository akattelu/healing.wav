return {
  new =
      function()
        return {
          amplitude = 50,           -- Damage
          wavelength = math.pi / 2, -- Arc Length
          frequency = 200,          -- Speed
          period = 1,               -- Cooldown
          range = 2,                -- Duration
        }
      end
}
