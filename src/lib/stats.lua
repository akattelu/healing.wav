return {
  new =
      function()
        return {
          amplitude = 1,            -- Damage
          wavelength = math.pi / 2, -- Arc Length
          frequency = 200,          -- Speed
          period = 0.1,             -- Cooldown
          range = 1,                -- Duration
        }
      end
}
