return {
  new =
      function()
        return {
          amplitude = 1,            -- Damage
          wavelength = math.pi / 2, -- Arc Length
          frequency = 100,          -- Speed
          period = 0.5,             -- Cooldown
          range = 1.5,              -- Duration
        }
      end
}
